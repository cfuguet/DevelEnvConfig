Install dircolors with the solarized theme:

	DIRCOLORS_PATH=~/.dircolors
	if [ -f ${DIRCOLORS_PATH} ]; then
		eval $(dircolors -b ${DIRCOLORS_PATH}) ;
	fi

Display the git branch on the bash prompt:

	$(__git_ps1 "(%s)")
